
describe('TESTANDO ALGUMA COISA', () => {
  it('should return one', () => {
    const number = 1;
    expect(number).toBe(1);
    // espero que variavel number seja diferente de 1 | usar not
    // expect(number).not.toBe(2);
  });
});

describe('TESTANDO ALGUMA COISA', () => {
  test('should return Matheus', () => {
    const nome = 'Matheus';
    expect(nome).toBe('Matheus');
  });
});


